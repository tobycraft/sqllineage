name: codspeed

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "commit / branch / tag to clone and benchmark (defaults to the triggering ref)"
        required: false
        type: string

jobs:
  walltime:
    # requires CodSpeed Macro Runners: GitHub org account with CodSpeed
    # enabled for this repository (see https://codspeed.io/docs/features/macro-runners)
    runs-on: codspeed-macro
    strategy:
      matrix:
        include:
          - backend: networkx
            test_filter: "test_tpcds_print_column_lineage and not rustworkx"
          - backend: rustworkx
            test_filter: "test_tpcds_print_column_lineage_rustworkx"
    steps:
      - name: Checkout sqllineage under test
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref || github.sha }}
      - name: Checkout benchmark harness
        uses: actions/checkout@v6
        with:
          path: codspeed-harness
      - uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: uv sync --project codspeed-harness --extra codspeed --no-install-project
      - name: Run benchmark (walltime, ${{ matrix.backend }})
        uses: CodSpeedHQ/action@v4
        with:
          mode: walltime
          token: ${{ secrets.CODSPEED_TOKEN }}
          run: PYTHONPATH=. uv run --project codspeed-harness --no-sync pytest codspeed-harness/benchmarks/codspeed -k "${{ matrix.test_filter }}" --codspeed
